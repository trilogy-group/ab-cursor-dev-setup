{
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },
    "install": "npm install",
    "start": "update-alternatives --set iptables /usr/sbin/iptables-legacy && update-alternatives --set ip6tables /usr/sbin/ip6tables-legacy && rm -rf /var/lib/docker/network && dockerd > /tmp/dockerd.log 2>&1 & sleep 8",
    "terminals": [
        {
            "name": "Supabase",
            "command": "supabase start"
        }
    ]
}